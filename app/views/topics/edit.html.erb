<%= render 'shared/navbar' %>
<div class="container">
  <h2>Modifier une discussion</h2>
  <div class="row">
    <div class="col-xs-offset-2 col-xs-8">
      <%= simple_form_for (@topic) do |f| %>
        <%= f.input :title, label: 'Titre', autofocus: true %>
        <%= f.input :info, label: 'Ajouter un commentaire' %>
        <%= f.button :submit, 'Ok' %>
      <% end %>
    </div>
  </div>
</div>
