<%= render 'shared/navbar' %>
<div class="container">
  <h2>Créer une discussion</h2>
  <div class="row">
    <div class="col-xs-offset-2 col-xs-8">
      <%= simple_form_for (@topic) do |f| %>
        <%= f.error_notification %>
        <%= f.input :title, label: 'Titre', placeholder: 'Titre', autofocus: true %>
        <%= f.input :info, label: 'Ajouter un commentaire', placeholder: 'Commentaire' %>
        <%= f.button :submit, 'Ok' %>
      <% end %>
    </div>
  </div>
</div>
